apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: production-api
  labels:
    app: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
